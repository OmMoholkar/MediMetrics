export const submitEmployeeData = async (employeeData) => {
    console.log('Data submitted:', employeeData);
    // Simulate a successful response
    return { success: true, message: 'Employee data submitted successfully' };
};
